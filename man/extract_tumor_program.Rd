% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolute.R
\name{extract_tumor_program}
\alias{extract_tumor_program}
\title{Extract Tumor-Specific Transcriptional Program from Deconvolution}
\usage{
extract_tumor_program(
  deconv_result,
  signatures,
  bulk_counts,
  gene_names = NULL,
  top_n = 500,
  return_all = FALSE
)
}
\arguments{
\item{deconv_result}{List returned from fit_deconv_em() containing 'exposures' and 'pred_counts'}

\item{signatures}{Matrix of cell type signatures used in deconvolution \%[\%\genes x cell_types\%[\%\}

\item{bulk_counts}{Matrix of observed bulk expression \%[\%\genes x samples\%[\%\}

\item{gene_names}{Optional vector of gene names (default: rownames of signatures)}

\item{top_n}{Number of top genes to return (default: 500)}

\item{return_all}{If TRUE, return all genes ranked; if FALSE, return top_n (default: FALSE)}
}
\value{
A list containing:
\itemize{
\item tumor_upregulated: Genes overexpressed in tumor vs normal hematopoiesis
\item tumor_downregulated: Genes underexpressed in tumor vs normal hematopoiesis
\item intercept_values: Per-sample intercept (noise) levels
\item residuals: Full residual matrix \%[\%\genes x samples\%[\%\
\item mean_residual: Mean residual per gene across samples
\item intercept_fraction: Fraction of total signal from intercept per sample
}
}
\description{
This function analyzes the intercept term from deconvolution to identify
genes that comprise the tumor-specific program - i.e., genes whose expression
cannot be explained by normal hematopoietic cell types.
}
\examples{
result <- fit_deconv_em(sigs, bulk, gene_lengths, w_vec, max_iter, tol, l1, verbose)
tumor_program <- extract_tumor_program(result, sigs, bulk)

# Top tumor-upregulated genes
head(tumor_program$tumor_upregulated)

# Visualize
plot(tumor_program$mean_residual, 
     main = "Tumor-Specific Expression Profile",
     xlab = "Gene Index", ylab = "Mean Residual")
}
