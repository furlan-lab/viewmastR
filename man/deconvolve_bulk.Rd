% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deconvolute.R
\name{deconvolve_bulk}
\alias{deconvolve_bulk}
\title{Deconvolve bulk RNA-seq data using single-cell signatures}
\usage{
deconvolve_bulk(
  signatures,
  bulk_counts,
  gene_lengths,
  gene_weights = NULL,
  backend = "wgpu",
  insert_size = 500,
  init_log_exp = -10,
  learning_rate = 0.01,
  l1_lambda = 0,
  l2_lambda = 0,
  max_iter = 10000,
  poll_interval = 100,
  ll_tolerance = 1e-06,
  sparsity_tolerance = 1e-04,
  verbose = TRUE,
  method = c("gd", "em")
)
}
\arguments{
\item{signatures}{Matrix of cellular signatures (genes × cell_types).
Each column should be normalized to sum to 1.}

\item{bulk_counts}{Matrix of bulk RNA-seq counts (genes × samples).
Gene names must match signatures.}

\item{gene_lengths}{Numeric vector of gene lengths, same length as nrow(signatures).}

\item{gene_weights}{Optional numeric vector of gene weights (0-1), same length as
nrow(signatures). Default is all 1s.}

\item{backend}{Which compute backend to use: "ndarray" (CPU, default),
"wgpu" (GPU if compiled with wgpu feature), "cuda" (CUDA if available).}

\item{insert_size}{Insert size for RNA-seq fragments. Default 500.}

\item{init_log_exp}{Initial value for log exposures. Default -10.}

\item{learning_rate}{Learning rate for optimization. Default 0.01.}

\item{l1_lambda}{L1 regularization parameter. Default 0.0 (no regularization).}

\item{l2_lambda}{L2 regularization parameter. Default 0.0 (no regularization).}

\item{max_iter}{Maximum number of iterations. Default 10000.}

\item{poll_interval}{Check convergence every N iterations. Default 100.}

\item{ll_tolerance}{Log-likelihood convergence tolerance. Default 1e-6.}

\item{sparsity_tolerance}{Sparsity convergence tolerance. Default 1e-4.}

\item{verbose}{emit learning progress stats}

\item{method}{either default - gd (Poisson regression with gradient descent) or em (Expectation-Maximization algorithm)}
}
\value{
A list with:
\item{exposures}{Matrix of cell type proportions (cell_types+intercept × samples)}
\item{pred_counts}{Matrix of predicted bulk counts (genes × samples)}
\item{proportions}{Normalized cell type proportions excluding intercept}
}
\description{
Deconvolve bulk RNA-seq data using single-cell signatures
}
\examples{
\dontrun{
# CPU backend (default)
result <- deconvolve_bulk(
  signatures = signatures,
  bulk_counts = bulk_counts,
  gene_lengths = gene_lengths
)

# GPU backend (if available)
result <- deconvolve_bulk(
  signatures = signatures,
  bulk_counts = bulk_counts,
  gene_lengths = gene_lengths,
  backend = "wgpu"
)
}
}
