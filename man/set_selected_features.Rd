% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_features.R
\name{set_selected_features}
\alias{set_selected_features}
\title{Set Selected Features for Ordering}
\usage{
set_selected_features(cds, genes, gene_column = "id", unique_column = "id")
}
\arguments{
\item{cds}{An object of class \code{cell_data_set} (Monocle3) or \code{Seurat}.}

\item{genes}{Character vector. A list of gene identifiers to mark as selected.
These must match the identifiers found in \code{gene_column}.}

\item{gene_column}{Character. The name of the column in the object's dispersion
metadata to match against the provided \code{genes} vector. Default is "id".}

\item{unique_column}{Character. Used only for Monocle3 objects with a dispersion function present.
Specifies the unique identifier column in the dispersion table to map back to if \code{gene_column}
is being used for lookup (e.g., mapping gene symbols back to Ensembl IDs). Default is "id".}
}
\value{
The modified \code{cds} object with the updated \code{use_for_ordering} slot.
}
\description{
This function updates the internal metadata of a Monocle3 (\code{cell_data_set}) or
Seurat object to mark specific genes as \code{use_for_ordering}. This is useful for
defining a custom set of features for trajectory inference.
}
\details{
Manually defines the set of features (genes) to be used for trajectory ordering or
downstream analysis.
}
\examples{
\dontrun{
  # Define a list of interesting genes
  my_genes <- c("GeneA", "GeneB", "GeneC")

  # Update the object to use these genes for ordering
  cds <- set_selected_features(cds, genes = my_genes)
}
}
