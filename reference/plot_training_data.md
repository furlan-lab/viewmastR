# Training data plot

This function will display a plot of data generated during viewmastR
training

## Usage

``` r
plot_training_data(output_list)
```

## Arguments

- output_list:

  a list returned from running viewmastR using the return_type = "list"
  parameter.

## Value

a plot of training data
