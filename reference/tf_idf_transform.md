# Performs TF-IDF transformation on a cell_data_set

Just like it sounds.

## Usage

``` r
tf_idf_transform(input, method = 1, verbose = T)
```

## Arguments

- cds_list:

  Input cell_data_set object or sparse matrix.
